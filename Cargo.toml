[package]
name = "dauntless"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0.228", optional = true, features = ["derive"] }

[dev-dependencies]
image = "0.25.6"
opencv = "0.97.2"

[features]
serde = ["dep:serde"]
